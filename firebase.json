{
  "hosting": {
    "public": "client/dist",
    "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
    "rewrites": [
      { "source": "/api**", "run": { "serviceId": "zync-api", "region": "asia-south1" } },
      { "source": "**", "destination": "/index.html" }
    ]
  },
  "firestore": {
    "indexes": "server/firestore.indexes.json"
  }
}
