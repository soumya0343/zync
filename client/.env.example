# Firebase: Project Settings > General > Your apps > SDK setup. Copy these from the config object.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Backend API URL (no trailing slash). Leave unset in production when using Firebase Hosting
# rewrites to Cloud Run (same origin /api). Set only when API is on another host (e.g. Render).
# VITE_API_URL=https://your-api.onrender.com
