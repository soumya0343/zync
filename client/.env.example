# Backend API URL (no trailing slash). Only needed for production build or when not using Vite proxy.
# Leave unset for local dev (Vite proxies /api to localhost:3000).
# VITE_API_URL=https://zync-api.onrender.com
