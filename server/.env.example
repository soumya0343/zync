# Server port (Render sets PORT automatically)
PORT=3000

# PostgreSQL connection string (e.g. from Neon or Supabase)
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# Secret for signing JWTs (use a strong random string, e.g. openssl rand -base64 32)
JWT_SECRET=your-jwt-secret-here

# development | production
NODE_ENV=development

# Frontend origin for CORS and cookies (production only; no trailing slash)
# Local dev: leave unset to default to http://localhost:5173
# FRONTEND_URL=https://your-app.vercel.app
