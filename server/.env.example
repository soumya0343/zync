# Server port
PORT=3000

# Firebase (required). Get from Firebase Console > Project Settings > Service accounts > Generate new private key.
# Option A: Path to service account JSON file
# GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json

# Option B: Paste the entire JSON as a single line (no newlines)
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"..."}

# Frontend origin for CORS (production only)
# FRONTEND_URL=https://your-app.vercel.app
